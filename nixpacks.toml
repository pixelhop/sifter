# Nixpacks configuration for pnpm monorepo

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm", "ffmpeg"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[start]
cmd = "echo 'Use service-specific start commands'"
