# Railway configuration for Sifter monorepo
# Services: api, workers (Redis and Postgres added via Railway dashboard)

[build]
builder = "railpack"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
